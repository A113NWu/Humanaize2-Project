Solve this task: {task_title}

Task description: {task_description}

Problem context: {problem}

{hsn_context}

MODE: {output_mode}

YOU MUST FOLLOW THESE RULES:

## COMMAND MODE (Default - Efficient)
When output mode is "command":
- Output ONLY executable commands or skill invocations
- NO explanations, NO descriptions, NO comments
- Output format for commands: just the command line(s)
- Output format for skills: {{"skill": "skill-name", "input": {{...}}}}
- Maximum 3 commands per response
- Prioritize shell commands, not text output

## TEXT MODE (Only when explicitly requested)
When output mode is "text":
- Generate the requested text content
- Follow the task requirements closely
- Keep output concise and to the point
- Can be longer if needed for comprehensive content

## CODE MODE (For project code generation)
When output mode is "code":
- Generate COMPLETE, production-quality code
- Include ALL necessary imports, classes, functions
- Write the FULL implementation - never truncate or abbreviate
- Use proper language-specific formatting and conventions
- If the task involves file operations, specify the file path in a comment
- Output format: pure code (no markdown, no explanations)
- The code must be complete enough to run/use immediately

## GENERAL RULES
- NEVER write placeholder code like "..." or "(and so on)"
- NEVER use "// ..." or "# ..." to indicate continuation
- NO Markdown formatting headers (#, *, -, 1.)
- NO introductions or explanations before the actual output
- NO filler phrases
- For command mode: keep response under 300 characters
- For text mode: can be longer as needed
- For code mode: generate the COMPLETE code, regardless of length

Output your solution now.
