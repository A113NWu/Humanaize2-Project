<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- 计算相关权限 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    
    <!-- Shizuku 權限宣告（Shizuku 會在 appops 中授予 shell 級別權限） -->
    <permission
        android:name="moe.shizuku.manager.permission.API_V23"
        android:protectionLevel="signature" />
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:name=".AizeApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AizeCompanion"
        tools:targetApi="34">

        <!-- ShizukuProvider：用於向 Shizuku 服務宣告本 app 的存在 -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:exported="true"
            android:multiprocess="false"
            android:permission="moe.shizuku.manager.permission.API_V23" />

        <!-- 向 Shizuku 查詢服務的 manifest 宣告 -->
        <queries>
            <package android:name="moe.shizuku.privileged.api" />
            <package android:name="moe.shizuku.manager" />
            <intent>
                <action android:name="moe.shizuku.manager.intent.action.SEND_VERSION" />
            </intent>
        </queries>
        
        <!-- 主 Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- 算力服务（前台服务） -->
        <service
            android:name=".service.ComputeService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        
        <!-- 设备发现服务 -->
        <service
            android:name=".service.DiscoveryService"
            android:exported="false" />
        
    </application>

</manifest>
